FROM golang:1.25-alpine

WORKDIR /app

# Install dependencies needed for tests
RUN apk add --no-cache gcc musl-dev

COPY go.mod go.sum ./
RUN go mod download
COPY . .

# Set default environment variables for testing
ENV GO111MODULE=on

CMD ["go", "test", "-v", "-count=1", "./e2e/..."]
