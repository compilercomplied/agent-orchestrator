[tools]
go = "1.23.3"
"go:golang.org/x/tools/gopls" = "0.20.0"

[tasks.build]
description = "Build the application"
run = "go build -o bin/agent-orchestrator main.go"

[tasks.run]
description = "Run the application"
run = "go run main.go"

[tasks.test]
description = "Run all tests"
run = "go test ./..."
